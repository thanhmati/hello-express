name: Staging CI/CD Workflow

on:
    push:
        branches: ["staging"]
    pull_request:
        branches: ["staging"]

jobs:
    call-reusableWorkflow:
        name: run the job
        uses: ./.github/workflows/reusableWorkflow.yml
        with:
            stage: "staging"
